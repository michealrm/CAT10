mov r1, r2
mov r12, r32
mov r2, $FF
mov r03, $BBBB
mov r2, [$AAAA]
mov [$1188], r3
mov r0, [r01]
mov [r01], r3

ADDC R1, R1
ADDC R2, $DD
ADDC R0, [$1234]
ADDC [$ABCD], R0

SUBB R1, R1
SUBB R2, $DD
SUBB R0, [$1234]
SUBB [$ABCD], R0

CMP R1, R1
CMP R2, $DD
CMP R0, [$1234]
CMP [$ABCD], R0

NOT R1
NOT [$ABCD]

AND R1, R1
AND R2, $DD
AND R0, [$1234]
AND [$ABCD], R0

OR R1, R1
OR R2, $DD
OR R0, [$1234]
OR [$ABCD], R0

XOR R1, R1
XOR R2, $DD
XOR R0, [$1234]
XOR [$ABCD], R0

push R0
pop R1